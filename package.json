{
  "name": "chv",
  "version": "0.0.1",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "webpack",
    "watch": "webpack -w",
    "package": "rm -rf build dist && webpack --mode=production && mkdir -p dist && cd build && zip -r ../dist/chv.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.106",
    "@types/iconv-lite": "^0.0.1",
    "copy-webpack-plugin": "^5.1.1",
    "ts-loader": "^7.0.2",
    "typescript": "^3.8.3",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "iconv-lite": "^0.5.1"
  }
}
